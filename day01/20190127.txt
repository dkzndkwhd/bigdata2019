http://cafe.daum.net/gardenpro
GITHUB 아이디, 저장장소 만들기

크롤링 : 웹사이트에서 데이터 긁어오는것
colaboratory : 파이선을 브라우저에서 바로 사용, 분석할 수 있는 기법 --> PC 아나콘다와 같은 기능 수행

진짜 데이터가 너무 많으면 하둡? 사용

데이터의 종류
- 정형 데이터 : 항목이 뚜렷한 데이터
 회원정보 ( 이름, 전화번호, 주소, 이메일 주소)
Database(db)MS : oracle , mySQL

- 반정형 데이터 : 항목 수가 제각각 다르다 
html , Xml , JSON
ex)
xml문서
구로경찰서
<addr> 구로구 벚꽃로</addr>
<location> 30, 100</location>
<tel>02-333-3333</tel>

JSON
addr : 구로구 벚꽃로
location : 30, 100
tel : 02-333-3333

- 비정형 데이터 : 이미지 동영상

파이선과 R이 알아먹을 수 있도록 데이터프레임을 씌워서 데이터를 입력

분석 : R
프로그래밍화 : 파이선



데이터 분석의 순서

수집{파일(엑셀, csv), DBMS(mySQL), 크롤링(비정형데이터-->xml, JSON)--> 전처리(이상한 데이터 처리<결측치>범위를 벗어나는 데이터 
(평균치 or 빼버리기)and 같은말인데 다르게 표현되어있는 말들 통합) --> 분석 --> 시각화

**DBMS : 서버임. 데이터를 달라고 요청해야 한다. 그걸 요청하는 언어:SQL -->정형 데이터**

판다스 - 파이선의 데이터 분석 라이브러리

R과 파이선은 프로그램을 부품조립식으로 함.
부품( 기능을 가지고 있음) 통계를 위한 부품, 그래프를 위한 부품 등등 필요한 부품을 다운받아야 한다.
RStudio 는 부품 설치가 메뉴화되어있다. 파이선은 pip 함수로 부품 불러옴.

Rstudio는 관리자 권한으로 실행해야 모든 기능을 사용할 수 있다. 속성- 고급 - 관리자 권한으로 실행 해놓으면 항상 관리자 권한으로 실행


[RStudio]

Rstudio 환경설정
tools에서 글로벌 옵션. 파일을 만들었을때 어디에 저장될것인지를 정할 수 있다(default working directory)

글로벌 옵션 - 코드 - saving - default text encoding 을 UTF-8로 변경

단축키:
ctrl shift 1234 모양새 바꾸기 1: R스크립트 2: R콘솔 4: 표준화면 
R스크립트 실행(Run : 컨트롤+enter)
콘솔 클리어 : ctrl + L

ctrl l -->clear

tab 자동완성 첫번째꺼 불러오기

띄어쓰기 해주는 것이 편하다

방향키로 이전에 쳤던 코드 불러올 수 있다.

이중따옴표와 따옴표 하나와 차이가 없다

사칙연산을 할 때만 숫자로 취급(나이 등)
나머지는 문자취급해야하는 데이터 ( 전화번호 등, 자리수가 중요하지 않은 것)

ex)
테이블 (회원정보)
이름         나이    연락처
김아무개    100      011
박아무개    200      019
송아무개    300      018
문자열      숫자      문자열

-->10가로로 봤을 때는 다른 형태의 데이터들이 들어 있지만
세로로 봤을 때는 같은 유형의 값들만 들어가도록 해야 분석의 대상이 될 수 있다.
모든 데이터는 테이블로 정형화해서 분석하는 것이 편하다.---> 데이터 프레임
가로 : 리스트 형태( 다른 유형의 데이터)
세로 : 벡터 형태( 같은 유형의 데이터)

같은 데이터라도 범주가 있고 없고에 따라 처리방식이 달라진다.

프린트 함수-->그냥 생략해도 찍힘

ex)
나이를 구하는 처리
입력값: 2000(year : 변하는 값이 들어가는 공간의 이름==>변수)
나이를 계산하는 것은 :2019 - 2000 +1
--> 입력값은 매번 달라진다.
so 나이(age) = 2019 - 입력값(year) + 1
결과값은 앞에 나와야 한다(등호) 값을 계산해서 결과값에 저장한다. (=등호: 변수에 데이터를 넣는 역할 -->대입연산자, 할당연산자)

CPU(계산)
저장공간RAM(입력값이 들어가는 부분)내의 데이터를 구분하기 위해서
				 저장공간에 이름을 부여하여 구분한다
				 저장공간에 이름 부여시 보통 영어소문자로 부여(맨앞에는 숫자가 오면 안된다)
				 year(입력값), age(출력값)


콘솔에서 한줄씩 써서 실행시키는것 : 스크립트

R script file 왼쪽에 있는 한번에 실행시키는 화면

부품 == 라이브러리
- R 설치시 기본적으로 라이브러리가 설치되어 있다. 
추가할 때에는 CRAN저장소에서 다운로드받아 설치

tools에서 install packages
dependancies : 그거를 실행하기 위해 필요한 다른 것도 같이 설치
package : ggplot2 그래프 그릴 때 가장 많이 사용하는 라이브러리
install.packages("ggplot2") 코드로 남겨두는 것이 편하다

package : glue
ex) 내 이름은 %%입력값$$ 처럼 하나로 붙여서 출력하기위한 모듈

자동완성 오른쪽에 써 있는게 출처 라이브러리 : 기본은 base 로 씌여 있다.

패키지 사용 위해서는 library()함수 사용

tools - check for package update : 설치된 패키지들 업데이트 시켜주는 기능

tools - 에서 modify keyboard shortcuts 단축기 변경 가능  comment: 주석  단축기 ctrl / 로 변경 --> 편하다

주석은 #


모든 데이터는 RAM 을 거친다 그 다음 cpu로 간다.

factor 는 계산의 대상이 되지 않는다. 요소의 성격에 따른 구분값이다. <--> scalar
factor 로 지정이 되면 수정이 불가능하다. 추가 불가. 다시 scalar로 변경 후 수정해야 한다.

ex) 쇼핑몰
회원관리
상품
게시판

my sql - 회원, 상품, 게시판
	특정한 회원이 특정한 상품을 구매
	특정한 회원이 게시판에 후기를 올림.
--> 관계를 따져서 DB 생성 --> RDB

변수 만들 때 ex)
myAGE - yourAge 자바 베이스 

mom_age - dad_age 파이선 베이스
다 소문자로 연결하면 나중에 오타 찾기 힘들어짐

eyes = c(10,33,44) 이런 형태 --> 벡터이다 c : combine

프로젝트와 스크립트 저장시 내문서에 저장된다

데이터 저장, 채굴시 github 사용

factor : 범주형. 이외의 데이터는 받아들이지 않는다 but factor 안에 있는 것만 변경이 허용된다.

str() 함수 : 현재 상태를 알려주는 함수 (structure)

as.  -> ~로 바꿔라

 ' . ' 점도 하나의 연산자이다 (접근 연산자) 더 깊게 함수에 접근

* 입력

2000
숫자를 입력했을 때
기본값은 무조건 스트링(문자열)
=> 숫자로 변환하고 싶다면??

==>scan() 자동으로 숫자로 변환시켜주는 함수 . 입력은 무조건 숫자로 해야 한다. what='' 옵션을 넣어야지 스트링을 넣을 수 있다.
벡터 형태로 통째로 집어넣을 수도 있다.

실수는 정수를 포함하는 개념. scan() 함수는 입력값을 실수로 받아들인다. 

정수 integar
실수 real(파이선과 다르다.) , float , double

str() - 디테일함
class() - 심플하게
typeof() - 데이터 유형에 대해서 더 디테일하게
모두 변수 형태를 알려주는 함수들

여러개 출력시 cat, glue 사용

package 불러와놓고 안쓰면 지워질수도 있다. library 함수 줄을 한번 더 실행해서 해결

공공데이터 포탈 : www.data.go.kr

데이타를 받는 방법 (2가지)

1) 파일로 제공 (엑셀, csv)
2) 검색어를 주면 찾아서 실시간으로 프로그램에 전달 (네트워크 프로그램 필요) 요청할 수 있는 키를 발급 ->그사람만 접근하도록
===> open API 라고 한다(함수, 방법) 제공하는 곳이 있고 아닌 곳이 있다. 없으면 크롤링 사용 대부분은 open api 제공 ->검색기록을 알 수 있다.
connect("http://www.data.go.kr?search="산")
북한산 은평구 700 02-55-7894
여러 가지 데이터가 있음- 메타데이터 ==> xml, JSON으로 변환해서 전달
name : 북한산
location : 은평구
hight : 700

Rstudio 에서 데이터 불러오기

environment 에서 import data set 
중간에 RAM 을 거친다
strings as factors -> 문자열로 읽어온다
명령어로 읽어오는 것이 훨씬 안정적이다. 































