CPU (처리, 연산)<----   주기억장치(쓸것만 입력됨)         <----- 보조 기억장치
		         (RAM)	            		수집(file, web) 
100 + 200
이름 A =100 : 저장공간의 이름(변수)
이름 B = 200 : 저장공간의 이름(변수)
A + B


내 입맞에 맞는 데이터를 만들러면 수집 + 수집 + 수집해서 재가공해야한다
구글 MAP(위치데이터) , 공공포탈  ==>재가공
ex)   (경찰서위치)	      (관할지역정보, 5대사건정보)

데이터 수집
- 정형 데이터 : 틀이 정해져 있는 데이터 ( 이미 테이블에 저장되어 있음) DB관리 시스템에 저장.
-반정형 데이터 :  HTML 어떻게 가져오느냐에 따라 형태가 바뀔 수 있다. , XML , JSON -> 크롤링 할거임. 1. 네트워크 연결 툴 사용해서 연결 , 2 테이블 만들기
- 비정형 데이터 : 음성, 그래픽, 동영상

분석을 하려면 무조건 데이터프레임 형태로 만들어야 한다
R - 테이블에 넣어라 (데이타프레임)

파이썬 - 데이터프레임으로 정리가 필요 
다양한 형태의 데이터 분석이 가능
프로그램 형태의 분석 시스템 구축 가능

테이블- 행과 열을 가진다
                열 : 내가 처리할 데이터 항목 :세로줄 -> **같은 타입의 데이터** => 벡터 형태
	행: 열에 따른 실제 데이터 : 가로줄 - > 다른 타입의 데이터의 조합& 같은 타입의 데이터 => 리스트 자료구조

ex) 
학생 테이블 : 이름 나이 연락처
	     김    100   011
	     박    50    012


## 파일의 형태

1. CSV 파일( 텍스트파일이지만 사이사이에 쉼표로 구분되어있다.
2. EXCEL 파일
3. JSON , XML 파일

data.go.kr

파일을 획득하는 방법
------------------------
1. 파일을 다운로드하면 됨.
2. 원격으로 접속하여 원하는 데이터를 요청.
open된 방법으로 데이터 open : dataSend()
--->openAPI 방식. 각각이 가지고 있는 메뉴얼의 방법을 사용해야 한다. 네이버, 다음, 구글 등등등


csv -> 쉼표로 구분된 파일 (메모장으로 만들고 저장시 .csv로 바꾸고 밑 옵션을 모든 파일로 바꾼다.
엑셀 읽으려면 다른 패키지 설치해야 한다
tools -> install packages -> readxl 검색 -> 다운로드

MS 사이트 엑셀에서 기본적인 분석들이 가능
구글에서도 자체적으로 만든 언어 사용중 **요즘 핫하다고 한다.

외부의 패키지를 사용할 때에는 library()함수로 불러와야 사용할 수 있다
==>라이브러리
1. 다운로드해서 설치 --> install.packages("")
2. 라이브러리 함수로 불러오기(사용하겠다 라고 셋팅) library("readxl")


파일 확장명 건드릴 때를 위해 탐색기에서 확장명이 나오도록 설정해 준다.
 
데이터분석용 : R, 파이선
변수 ---> 연산자 --->제어문 --->함수

# new packages  --> "lubridate"

구글 colab --> 성능을 빌려 쓸 수 있다.























